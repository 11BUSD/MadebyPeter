# ADR 0002: React Flow Map rendering

Status: Accepted

Use `@xyflow/react` in a dynamically loaded client component. Story view server-renders first; Map requests only a capped neighborhood and always exposes an equivalent semantic list. Layout coordinates are presentation state, not canonical node content.
