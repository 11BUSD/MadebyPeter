"use client";
import {useState,type FormEvent} from "react";import type {z} from "zod";import type {captureOutputSchema} from "@/domain/agents/contracts";import {mediaUrlSchema} from "@/domain/media/providers";
type Draft=z.infer<typeof captureOutputSchema>;type Kind="text"|"voice"|"photo"|"link"|"song";
export function CaptureFlow({source,published}:{source?:string;published:boolean}){const[step,setStep]=useState<1|2|3>(1);const[kind,setKind]=useState<Kind>(source?"text":"text");const[text,setText]=useState(source?`A new direction growing from ${source}`:"");const[url,setUrl]=useState("");const[file,setFile]=useState<File|null>(null);const[draft,setDraft]=useState<Draft|null>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[saved,setSaved]=useState(published);
async function createDraft(event:FormEvent){event.preventDefault();setBusy(true);setError("");try{if(kind==="song"&&url&&!mediaUrlSchema.safeParse(url).success)throw new Error("Use a public SoundCloud, Spotify, or Apple Music HTTPS link.");const form=new FormData();form.set("kind",kind);form.set("text",text);if(url)form.set("url",url);if(file)form.set("file",file);const response=await fetch("/api/agents/capture",{method:"POST",body:form});const body=await response.json();if(!response.ok)throw new Error(body.error);setDraft(body.output);setStep(2)}catch(cause){setError(cause instanceof Error?cause.message:"Could not prepare the draft.")}finally{setBusy(false)}}
  if(saved)return <section className="panel stack" aria-live="polite"><p className="eyebrow">Saved locally for review</p><h2>Your draft is ready.</h2><p>Fixture mode does not persist accounts or ideas. Configure Supabase and sign in to publish canonical content.</p><button onClick={()=>{setSaved(false);setStep(1)}}>Add another</button></section>;
  return <section className="capture panel stack" aria-label={`Capture step ${step} of 3`}><p className="fine">Step {step} of 3 · deterministic draft helper</p>{step===1&&<form onSubmit={createDraft} className="stack"><fieldset><legend>How do you want to start?</legend><div className="capture-kinds">{(["text","voice","photo","link","song"] as Kind[]).map(value=><label key={value} className={kind===value?"selected":""}><input type="radio" name="kind" value={value} checked={kind===value} onChange={()=>setKind(value)}/><span>{value==="text"?"Type":value==="voice"?"Speak":value==="photo"?"Photo":value==="link"?"Link":"Song"}</span></label>)}</div></fieldset>{kind==="voice"&&<><label htmlFor="voice">Voice note</label><input id="voice" type="file" accept="audio/mpeg,audio/mp4,audio/wav,audio/webm,audio/ogg" onChange={e=>setFile(e.target.files?.[0]||null)}/><p className="fine">Maximum 20 MB. The local adapter does not transcribe audio.</p></>}{kind==="photo"&&<><label htmlFor="photo">Photo</label><input id="photo" type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>setFile(e.target.files?.[0]||null)}/></>}{(kind==="link"||kind==="song")&&<><label htmlFor="capture-url">{kind==="song"?"SoundCloud, Spotify, or Apple Music link":"Public HTTPS link"}</label><input id="capture-url" type="url" value={url} onChange={e=>setUrl(e.target.value)} required/></>}<label htmlFor="capture-text">{kind==="text"?"What is the idea?":"Add a short note"}</label><textarea id="capture-text" value={text} onChange={e=>setText(e.target.value)} rows={5} required={kind!=="voice"} maxLength={10000}/>{error&&<p role="alert" className="error">{error}</p>}<button disabled={busy}>{busy?"Preparing…":"Prepare my draft"}</button></form>}{step===2&&draft&&<div className="stack"><p className="eyebrow">Draft for your approval</p><label htmlFor="draft-title">Is this title right?</label><input id="draft-title" value={draft.title} onChange={e=>setDraft({...draft,title:e.target.value})} maxLength={160}/><label htmlFor="draft-summary">One-sentence idea</label><textarea id="draft-summary" value={draft.summary} onChange={e=>setDraft({...draft,summary:e.target.value})} rows={3} maxLength={400}/>{draft.missingQuestions.map(question=><p className="notice" key={question}>{question}</p>)}<p className="fine">Draft-only · local-rules-v1 · no sources used · estimated cost $0</p><div className="actions"><button className="secondary" onClick={()=>setStep(1)}>Back</button><button onClick={()=>setStep(3)}>Looks right</button></div></div>}{step===3&&draft&&<div className="stack"><p className="eyebrow">Ready when you are</p><h2>{draft.title}</h2><label htmlFor="visibility">Who can see it?</label><select id="visibility" defaultValue="private"><option value="private">Only me</option><option value="unlisted">Anyone with the link</option><option value="public">Everyone</option></select><label className="check"><input type="checkbox"/> Publish now (otherwise keep as a draft)</label><p className="fine">Publishing is a human action. The draft helper cannot publish for you.</p><div className="actions"><button className="secondary" onClick={()=>setStep(2)}>Back</button><button onClick={()=>setSaved(true)}>Save idea</button></div></div>}</section>}
