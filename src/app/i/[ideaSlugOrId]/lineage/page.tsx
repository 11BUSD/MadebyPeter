import Link from "next/link";import {getIdea} from "@/lib/public-data";
export default async function LineagePage({params}:{params:Promise<{ideaSlugOrId:string}>}){const{ideaSlugOrId}=await params;const idea=await getIdea(ideaSlugOrId);return <main id="main" className="narrow stack"><p className="breadcrumbs"><Link href={`/i/${idea.slug}`}>{idea.title}</Link> / Lineage</p><p className="eyebrow">Where this came from</p><h1>{idea.title}</h1><section className="panel stack"><span className="tag">Original in this fixture</span><h2>Created by {idea.creator.displayName}</h2><p>This synthetic idea has no recorded source. When someone references, forks, or remixes it, the platform records the exact source version, creator, licence snapshot, mode, and time.</p></section><p className="fine">Lineage is an attribution record. It does not itself grant a licence, royalty, equity, partnership, or legal ownership.</p><Link className="button" href={`/new?source=${idea.slug}`}>Grow this idea</Link></main>}
