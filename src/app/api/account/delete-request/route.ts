import {NextResponse} from "next/server";import {createSupabaseServerClient} from "@/lib/supabase/server";
export async function POST(request:Request){const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)return NextResponse.json({error:"Cross-origin request blocked"},{status:403});const supabase=await createSupabaseServerClient();if(!supabase)return NextResponse.redirect(new URL("/settings/account?status=fixture",request.url),303);const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.redirect(new URL("/auth/sign-in?next=/settings/account",request.url),303);await supabase.rpc("request_account_deletion");return NextResponse.redirect(new URL("/settings/account?status=deletion-requested",request.url),303)}
