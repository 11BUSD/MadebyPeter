import Link from "next/link";
import { IdeaCard } from "@/components/idea-card";
import { getFeaturedIdeas } from "@/lib/public-data";

export default async function Home(){const featured=await getFeaturedIdeas();return <main id="main"><div className="shell"><section className="hero"><div><p className="eyebrow">A branchable record of ideas</p><h1>Ideas grow better with their roots intact.</h1></div><div><p className="lede">Explore what Peter is learning, making, and leaving open for someone else to grow.</p><div className="actions"><Link className="button" href="/g/energy-systems">Explore ideas</Link><Link className="button secondary" href="/new">Add an idea</Link></div></div></section><section className="section" aria-labelledby="featured"><div className="section-head"><div><p className="eyebrow">Start with a story</p><h2 id="featured">Inside the LNG lifecycle</h2></div><Link href="/u/peter">Meet Peter →</Link></div><div className="idea-grid">{featured.map((idea)=><IdeaCard key={idea.id} idea={idea}/>)}</div></section></div></main>}
