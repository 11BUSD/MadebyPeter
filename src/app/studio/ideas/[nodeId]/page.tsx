import Link from "next/link";import {notFound,redirect} from "next/navigation";import {createSupabaseServerClient} from "@/lib/supabase/server";
export default async function StudioIdea({params}:{params:Promise<{nodeId:string}>}){const{nodeId}=await params;const supabase=await createSupabaseServerClient();if(!supabase)return <main id="main" className="narrow"><h1>Idea editor</h1><p className="notice">Configure Supabase to edit canonical ideas.</p></main>;const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/auth/sign-in?next=/studio");const{data:node}=await supabase.from("nodes").select("id,graph_id,title,summary,status,visibility,current_version_id").eq("id",nodeId).single();if(!node)notFound();return <main id="main" className="narrow stack"><p className="breadcrumbs"><Link href={`/studio/graphs/${node.graph_id}`}>Graph</Link> / Idea</p><span className="tag">{node.status}</span><h1>{node.title}</h1><p className="lede">{node.summary}</p><div className="panel"><p>Version <code>{node.current_version_id}</code> is immutable. Editing creates a new version; that extension is intentionally deferred from this compact MVP UI.</p></div></main>}
