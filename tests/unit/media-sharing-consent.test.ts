import {describe,expect,it} from "vitest";import {mediaUrlSchema} from "@/domain/media/providers";import {createSharePayload} from "@/domain/sharing/payload";import {transitionConsent} from "@/domain/identity/consent";
describe("public boundaries",()=>{it.each([["https://soundcloud.com/artist/track","soundcloud"],["https://open.spotify.com/track/abc","spotify"],["https://music.apple.com/ca/album/example/1","apple_music"]])("accepts allowlisted music %s",(url,provider)=>expect(mediaUrlSchema.parse(url).provider).toBe(provider));it("rejects lookalike and non-HTTPS embed URLs",()=>{expect(mediaUrlSchema.safeParse("https://soundcloud.com.evil.test/track").success).toBe(false);expect(mediaUrlSchema.safeParse("http://soundcloud.com/track").success).toBe(false)});it("creates canonical share copy",()=>{const result=createSharePayload({title:"Idea",summary:"A useful thought",creator:"peter",canonicalUrl:"https://example.com/i/idea"});expect(result.url).toBe("https://example.com/i/idea");expect(result.text).toContain("@peter");expect(result.shortCaption.length).toBeLessThanOrEqual(280)});it("records reversible consent transitions",()=>{const granted=transitionConsent(undefined,true,"2026-08-02T00:00:00Z");expect(granted.withdrawnAt).toBeNull();const withdrawn=transitionConsent(granted,false,"2026-08-03T00:00:00Z");expect(withdrawn.status).toBe("withdrawn");expect(withdrawn.withdrawnAt).toBe("2026-08-03T00:00:00Z")})});
