import {describe,expect,it} from "vitest";import {boundedNeighborhood,wouldCreatePartOfCycle} from "@/domain/graph/algorithms";import {ideaInputSchema} from "@/domain/graph/model";import type {PublicEdge} from "@/domain/graph/model";
const edges:PublicEdge[]=[{id:"1",sourceNodeId:"b",targetNodeId:"a",relationType:"part_of"},{id:"2",sourceNodeId:"c",targetNodeId:"b",relationType:"part_of"}];
describe("graph integrity",()=>{it("rejects invalid and oversized ideas",()=>{expect(ideaInputSchema.safeParse({title:"",summary:"x",description:"",visibility:"private"}).success).toBe(false);expect(ideaInputSchema.safeParse({title:"Valid",summary:"x".repeat(401),description:"",visibility:"public"}).success).toBe(false)});it("detects direct and transitive part_of cycles",()=>{expect(wouldCreatePartOfCycle(edges,"a","c")).toBe(true);expect(wouldCreatePartOfCycle(edges,"a","a")).toBe(true);expect(wouldCreatePartOfCycle(edges,"d","c")).toBe(false)});it("caps traversal and filters edges outside the visible set",()=>{const nodes=["a","b","c","d"].map(id=>({id}));const many=[...edges,{id:"3",sourceNodeId:"d",targetNodeId:"a",relationType:"related_to" as const}];const result=boundedNeighborhood(nodes,many,"a",2);expect(result.nodes).toHaveLength(2);expect(result.truncated).toBe(true);expect(result.edges.every(edge=>result.nodes.some(node=>node.id===edge.sourceNodeId)&&result.nodes.some(node=>node.id===edge.targetNodeId))).toBe(true)})});
